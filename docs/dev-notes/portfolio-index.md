# Portfolio Notes Index

> 포트폴리오 정리 시 다시 읽기 위한 개발 기록 인덱스.
> 자세한 구현 설명보다, 나중에 "무엇을 왜 만들었는지"를 빠르게 뽑아내는 용도다.

## Search

- #49 LIKE 기반 성경 검색 API
- #50 성경 검색 화면 및 책 바로가기 UX
- #53 PostgreSQL FTS 기반 검색 개선

포트폴리오 포인트:
- 검색 API 계약을 먼저 만들고, 프론트 UX를 붙인 뒤, DB 검색 방식을 개선하는 단계적 개발 흐름
- API 응답 형식을 유지하면서 내부 검색 구현을 교체하는 구조
- PostgreSQL FTS, GIN index, 검색 성능 개선 학습

## Auth & Sync

- Supabase Auth + Spring Boot JWKS 검증
- 게스트 localStorage / 로그인 API 저장소 분리
- 첫 로그인 시 하이라이트 bulk sync

포트폴리오 포인트:
- 외부 인증 제공자 Supabase와 Spring Security 연동
- local-first UX와 로그인 후 클라우드 동기화 흐름
- 게스트 사용자 경험을 유지하면서 로그인 사용자 데이터 영속화

## User Data

- 하이라이트 CRUD
- 하이라이트 bulk sync
- #121 하이라이트 반응 속도 및 API 동기화 안정화
- 사용자 설정 preferences API
- 로그인 유저 설정 저장소 API 전환
- #67 저장한 말씀 API
- #68 마이페이지 사용자 화면 개선

포트폴리오 포인트:
- 사용자별 데이터 분리
- `verse_key` 기반 성경 구절 사용자 데이터 연결
- 프론트 localStorage 저장소에서 백엔드 API 저장소로 점진적 전환
- API 저장소 전환 후 optimistic UI와 실패 롤백으로 하이라이트 체감 속도 개선
- `UNIQUE(user_id, verse_key)`로 저장한 말씀 중복을 DB 레벨에서 방지
- 읽기, 저장, 내 목록 확인, 저장 해제를 하나의 사용자 흐름으로 연결

## AI Content Pipeline

- #85 AI 장 요약 배치 파이프라인
- #87 AI 요약 후보 저장과 문장흐름 검수
- #91 장별 요약 후보 소규모 배치 검증
- #95 창세기 장별 요약 후보 배치
- #96 OpenAI 장별 요약 생성 설정 최적화
- #97 전체 장별 AI 요약 생성 운영 메모
- #102 책 요약 생성 프롬프트와 66권 데이터 준비
- #112 인물 해설 MVP 샘플

포트폴리오 포인트:
- 장별 요약을 실시간 LLM 호출이 아니라 사전 생성 + DB 조회 구조로 설계해 런타임 비용과 응답 지연을 제거
- Spring Boot 배치 안에서 본문 수집, AI 요청, rule validation, 후보 저장, 승인 반영 흐름을 구성
- `chapter_summary_candidates`와 `chapter_summaries`를 분리해 생성형 AI 결과가 바로 서비스 데이터가 되지 않도록 품질 게이트 설계
- 1,189개 장 요약을 50개 단위로 생성/확인하고, `PASS + PENDING` 후보만 일괄 승인해 전체 장 요약 완성
- Gemini, Groq, OpenAI 모델을 실제 제약 기준으로 비교하고, 최종적으로 `gpt-5-nano`와 rule validation 중심 운영으로 정리
- 66권 책 요약은 장 요약과 달리 정적 upsert + 수동 검토 방식이 더 적합하다고 판단해 데이터 규모별 운영 방식을 다르게 선택

## Reading Plan

- #162 개인 통독 MVP 기획
- #164-168 개인 통독 백엔드 MVP

포트폴리오 포인트:
- 무료 서버 운영 제약을 고려해 단체 통독보다 개인 통독 MVP를 먼저 구현하는 단계적 제품 전략
- `reading_plans`, `reading_plan_items`, `reading_progress`로 계획과 완료 기록을 분리한 데이터 모델링
- partial unique index로 사용자당 진행 중 계획 1개 제한을 DB 레벨에서 보장
- `item_order`와 `day_number`를 분리해 전체 읽기 순서와 일차별 UI를 동시에 지원
- 장 단위 완료 기록을 이후 단체 통독의 멤버별 진행률로 확장 가능한 형태로 설계
- 서비스 단위 테스트로 생성, 조회, 삭제, 장 완료 및 중복 예외 흐름 검증

## Bible Translation

- #180 Phase 13 영어 역본 비교 기획
- #182 WEBP 성경 데이터 변환 및 검증
- #183 WEB 역본 비교 UI

포트폴리오 포인트:
- Public Domain WEBP 본문을 기존 `verse_key` 구조에 맞춰 정적 JSON으로 추가
- Render 서버 콜드스타트와 무관하게 한국어/영어 대조 본문을 읽을 수 있는 local-first 구조 유지
- 기본 본문 변경은 후속으로 미루고, MVP에서는 한국어 본문 중심의 대조 역본만 제공해 기능 범위 절제
- 하이라이트, 저장, 통독 같은 사용자 데이터는 계속 `verse_key`를 기준으로 연결
- WEB 원문 따옴표처럼 화면상 어색할 수 있는 부분도 원문성 유지 정책으로 판단

## Audio / TTS

- #187 Phase 14 TTS 기획
- #188 성경 읽기 TTS MVP
- #190 TTS QA 결과와 서버 확장 검토

포트폴리오 포인트
- 외부 TTS API 없이 브라우저 Web Speech API로 서버 비용 없는 듣기 MVP 구현
- 스크롤 기반 장 이어읽기 구조에서 URL 기준 장과 실제 화면 기준 장이 달라지는 문제를 재생 대상 계산으로 해결
- 듣기 중 구절 클릭, 이전/다음 절, 일시정지, 속도 조절을 모바일 하단 플레이어로 정리
- 대조 역본, 하이라이트, 통독 읽기 흐름과 충돌하지 않도록 기존 읽기 화면 위에 듣기 모드를 점진적으로 확장
- 서버 TTS 확장 전에 성경 본문 사용권, 합성 음성 저장/재사용, provider 문자 과금, 캐시 설계를 먼저 검토하는 운영 의사결정 흐름 정리

## Data Policy

- #194 성경 본문 저작권 및 공개 데이터 정책

포트폴리오 포인트:
- 실제 교회 사용성 때문에 운영 버전은 저작권 보호 대상 한국어 본문 기준으로 유지하되, 포트폴리오 공개 버전에서는 본문 데이터를 제외하는 정책 수립
- 실서비스 private repo와 public portfolio repo를 분리해 사용자 요구와 공개 리스크를 함께 관리
- README와 면접 설명에서 데이터 저작권, 샘플 데이터, public domain 역본 사용 전략을 명확히 설명할 수 있도록 정리

## Frontend UX

- #50 성경 검색 화면 및 책 바로가기 UX
- #68 마이페이지 사용자 화면 개선
- #120 백엔드 콜드스타트 대기 안내 및 중복 요청 방지
- #121 하이라이트 반응 속도 및 API 동기화 안정화

포트폴리오 포인트:
- 개발 중간 상태의 설명형 화면을 실제 사용자 기능 중심 화면으로 재구성
- 프로필, 설정, 저장한 말씀을 분리해 모바일 앱의 정보 구조 개선
- 저장한 말씀의 연속 구절 묶음, 부분 저장 성공 메시지, 확인 시트 등 실제 사용성을 고려한 상태 처리
- 임시 홈 말씀 이미지 CTA로 이후 주간 말씀 이미지 기능의 UI 방향을 선검증
- 무료 배포 환경의 콜드스타트 제약을 사용자 안내와 중복 요청 방지 UX로 완화
- 서버 응답을 기다리지 않는 optimistic update로 읽기 화면의 즉시 반응성 개선

## API Quality & Operations

- #55 프로덕션 환경 설정 분리
- #56 전역 예외 처리 정리
- #57 CORS 및 배포 헬스체크 정리
- #61 Dockerfile 및 배포 실행 설정 추가
- #62 Render 배포 및 프로덕션 환경변수 연결
- #63 API Rate Limit 적용
- #120 백엔드 콜드스타트 대기 안내 및 중복 요청 방지

포트폴리오 포인트:
- 개발/운영 설정을 분리하고 프로덕션 DB 스키마 검증 전략을 적용
- API 실패 응답을 공통 JSON 형식으로 표준화
- 400, 404, 409, 500 상태 코드를 상황별로 구분해 프론트엔드 연동 안정성 개선
- 프론트엔드/백엔드 분리 배포 환경에서 CORS와 health check를 운영 관점으로 정리
- 멀티스테이지 Dockerfile로 빌드 환경과 실행 환경을 분리
- Render 백엔드 배포와 Vercel 프론트 환경변수 연결로 실서비스 API 연동 검증
- 공개 API에 IP 기반 rate limit을 적용해 과도한 요청으로부터 서버와 DB를 보호
- `429`, `409` 같은 API 상태 코드를 프론트 사용자 문구와 요청 제어 흐름으로 연결

## Content & Re-Engagement

- #133 오늘의 말씀 후보 테이블 및 조회 API

포트폴리오 포인트:
- 전체 성경 랜덤 대신 큐레이션 후보군을 사용해 알림 콘텐츠 품질 리스크를 줄임
- `verse_key`를 공통 키로 유지하면서 DB FK로 후보 데이터 무결성 보강
- `sort_order + 날짜 계산`으로 별도 히스토리 테이블 없이 104일 순환 구조 구현
- 홈 화면과 PWA 알림이 같은 오늘의 말씀 소스를 재사용할 수 있는 기반 마련
